import React from 'react';

const Header = () => {
    return(
        <div className = "hero is-bold is-dark">
            <div className = "container">
                <div className = "hero-body">
                    <h1 className = "title">Go!</h1> 
                    <p className = "subtitle">Built with Bulma, React, and love. :)</p>
                </div>
            </div>
        </div>
    );
}


export default Header;